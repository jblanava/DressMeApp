<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dressmeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Modificar_Prenda"></activity>
        <activity android:name=".Activities.AgruparPrendasActivity" />
        <activity android:name=".Activities.OrdenarPrendasActivity" />
        <activity android:name=".Activities.AniadirPrendaActivity" />
        <activity android:name=".Activities.ConfiguracionActivity" />
        <activity android:name=".Activities.ConfiguracionContraseniaActivity" />
        <activity android:name=".Activities.RegistroActivity" />
        <activity android:name=".Activities.RecomendadorActivity" />
        <activity android:name=".Activities.VestuarioActivity" />
        <activity android:name=".Activities.MenuPrincipalActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>